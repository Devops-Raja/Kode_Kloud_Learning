 Passwordless login:

  ssh-keygen -t rsa  ( Create a key in present eg:jumphost server )

  ssh-copy-id sheena@192.168.0.11  ( copy the keys into another server (authorized keys) )

  Now user sheena can be able to login passwordless from the jumphost server)



  Copy your keys into remote server:
  ssh-copy-id -i ~/.ssh/id_ed25519.pub user@remote.server.ip

  # make sure you have right permission on the .ssh/authrorized_keys  (file/path permissions)
  # recommended permissions:
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
